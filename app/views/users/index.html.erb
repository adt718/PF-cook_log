<% provide(:title, 'ユーザー一覧') %>
<div class="container">
  <h1>ユーザー一覧</h1>
  <%= will_paginate %>
  <ul class="users">
    users: <%= @users %>
<%= render 'users', users: @users %>
  <%= will_paginate %>
</ul>
</div>