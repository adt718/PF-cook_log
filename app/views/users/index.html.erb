<% provide(:title, 'ユーザー一覧') %>
  <div class="container">
    <h1>ユーザー一覧</h1>
    <%= will_paginate @users %>
    <ul class="users">
      <%= render 'users', users: @users %>
    </ul>
    <div class="pagination">
    <%= will_paginate @users %>
    </div>
  </div>
